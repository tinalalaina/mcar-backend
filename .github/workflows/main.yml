name: Build and Push Docker Image

on:
  push:
    branches:
      - main
  workflow_dispatch: # Allow manual triggers

jobs:
  build:
    runs-on: ubuntu-latest

    services:
      docker:
        image: docker:24.0.7-dind
        options: --privileged

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to Docker Hub
        run: docker login -u shop784 -p ${{ secrets.DOCKERHUB_TOKEN }}
      
      - name: Extracts metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: shop784/mcar-back

      - name: Build and push Docker image
        run: |
          docker build -t shop784/mcar-back:latest .
          docker push shop784/mcar-back:latest


  # deploy:
  #     runs-on: ubuntu-latest
  #     needs: build
  #     steps:
  #     # Déployez sur le serveur distant
  #     - name: Deploy Docker image to remote server
  #       run: |
  #         sudo apt-get update && sudo apt-get install -y sshpass
  #         sshpass -p "${{ secrets.SERVER_PASSWORD }}" ssh -o StrictHostKeyChecking=no root@${{ secrets.SERVER_IP }} <<EOF
  #           docker pull ${{ secrets.DOCKER_USERNAME }}/mcar-back:latest
  #           docker stop andersonbackend || true
  #           docker rm andersonbackend || true
  #           docker system prune -f
  #           docker run -d \
  #             --name andersonbackend \
  #             --network anderson_network \
  #             --ip=172.19.0.3 \
  #             --restart unless-stopped \
  #             -p 8000:8000 \
  #             -v /srv/app_assets:/app/assets/ \
  #             -v /srv/app_staticfiles:/app/staticfiles/ \
  #             shop784/mcar-back:latest \
  #             sh -c "python manage.py collectstatic --noinput && \
  #                    python manage.py makemigrations --noinput && \
  #                    python manage.py migrate --noinput && \
  #                    python manage.py runserver 0.0.0.0:8000"
  #         EOF